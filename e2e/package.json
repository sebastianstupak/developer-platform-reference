{
  "name": "developer-platform-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end (Playwright) tests for the Developer Platform Web UI.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
